{
  "name": "deployready-app",
  "version": "1.0.0",
  "description": "Simple Node.js API for the DeployReady challenge (health, metrics, data endpoints)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
